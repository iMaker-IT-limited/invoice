import { BypasssanitizeDirective } from './bypasssanitize.directive';

describe('BypasssanitizeDirective', () => {
  it('should create an instance', () => {
    const directive = new BypasssanitizeDirective();
    expect(directive).toBeTruthy();
  });
});
